---
- name: Install/update neovim
  homebrew:
    name: "{{ item }}"
    state: latest
  loop:
  - neovim

- name: Install neovim plugins dependencies
  homebrew:
    name: "{{ item }}"
    state: latest
  loop:
  - git
  - golang
  - python-setuptools
  - python3-setuptools
  - python3-dev
