---
- name: Install neovim
  homebrew:
    name: neovim
    state: head

- name: Install neovim plugins dependencies
  homebrew:
    name: "{{ nvim_osx_dependencies }}"
    state: present

- name: Install virtualenv
  pip:
    name: "virtualenv"
    executable: "pip3"
    state: present
